{
  "name": "fanren-xiu-ai",
  "version": "1.0.0",
  "private": true,
  "description": "凡人修AI静态品牌官网与Markdown教程系统",
  "scripts": {
    "build": "node scripts/build-site.js",
    "build:vercel": "npm run build && node scripts/prepare-deploy.js",
    "seed:content-map": "node scripts/seed-content-map.js",
    "test": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test tests/*.test.mjs"
  }
}
